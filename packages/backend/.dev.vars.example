# 1delta Portal API key — used as x-api-key header on every request to portal.1delta.io
# Get a key at https://auth.1delta.io
# Without a key, requests are made without authentication (lower rate limits).
ONEDELTA_API_KEY=
